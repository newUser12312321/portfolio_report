<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= post.title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1><%= post.title %></h1>
    <p><strong>작성자:</strong> <%= post.author %> | <strong>조회수:</strong> <%= post.views %> | <strong>날짜:</strong> <%= new Date(post.created_at).toLocaleString() %></p>
    <hr>
    <div style="min-height: 200px;">
        <%= post.content %>
    </div>
    <hr>
    
    <a href="/"><button>목록으로</button></a>
    <a href="/edit/<%= post.id %>"><button>수정</button></a>
    
    <form action="/delete/<%= post.id %>?_method=DELETE" method="POST" style="display:inline;">
        <button type="submit" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</button>
    </form>
</body>
</html>