<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>글 수정</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>📝 글 수정</h1>
    <form action="/edit/<%= post.id %>?_method=PUT" method="POST">
        <p><input type="text" name="title" value="<%= post.title %>" required style="width: 100%;"></p>
        <p><input type="text" name="author" value="<%= post.author %>" required></p>
        <p><textarea name="content" rows="10" style="width: 100%;" required><%= post.content %></textarea></p>
        <button type="submit">수정 완료</button>
        <a href="/post/<%= post.id %>"><button type="button">취소</button></a>
    </form>
</body>
</html>